apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-auth-db
  labels:
    app: auth-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: default