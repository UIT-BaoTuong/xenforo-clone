apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-forum-db
  labels:
    app: forum-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: default